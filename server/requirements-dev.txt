# Test-only dependencies for the self-hosted server. Install on top of
# requirements.txt: `pip install -r requirements.txt -r requirements-dev.txt`.
-r requirements.txt

pytest>=8.0,<9.0
# Starlette's TestClient needs httpx.
httpx>=0.27,<1.0
