fastapi==0.115.8
uvicorn==0.34.0
pydantic[email]==2.10.4
mem0ai>=0.1.48
python-dotenv==1.2.2
psycopg>=3.2.8
psycopg-pool>=3.2.6,<4.0.0

# spaCy powers mem0's v3 entity extraction, which populates the entity store
# that the /graph endpoint reads. mem0ai already depends on spaCy, but pin it
# explicitly so the build is deterministic. The en_core_web_sm model itself is
# NOT a pip dependency - it is installed in the Dockerfiles via
# `python -m spacy download en_core_web_sm`. Without that model, entity
# extraction silently yields nothing and the graph stays empty.
spacy>=3.7.0,<4.0.0

# Bundled LLM/embedder providers. Extend by adding the provider's package
# here, rebuilding the image, and updating BUNDLED_*_PROVIDERS in main.py.
anthropic>=0.39,<1.0
google-generativeai>=0.8,<1.0

# Auth & database (Phase 1)
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
passlib[bcrypt]>=1.7,<2.0
# Load-bearing pin, not arbitrary. passlib 1.7.4 probes bcrypt.__about__, which
# bcrypt removed in 4.1. The probe failure is swallowed and the next hash() call
# fails with a misleading "password cannot be longer than 72 bytes" - on inputs
# well under 72 bytes - which breaks API key creation and login. Do not relax
# this without moving off passlib; existing password hashes must still verify.
bcrypt==4.0.1
python-jose[cryptography]>=3.3,<4.0
cryptography>46.0.4

# Anonymous telemetry — on by default, opt out with MEM0_TELEMETRY=false
posthog>=3.0,<8.0

# Per-IP rate limiting on auth endpoints
slowapi>=0.1.9,<1.0
